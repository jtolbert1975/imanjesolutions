{{#my-modal title='Quotes' ok='save' close='removeModal'}}
    <form {{action 'ok' on='submit' target=view}}>
        <div class="form-group">
            <label>Name</label>
            {{input class="form-control" value=record.name}}
        </div>
    </form>
{{/my-modal}}
